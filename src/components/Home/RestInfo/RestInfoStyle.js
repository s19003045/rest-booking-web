const RestInfoStyle = (theme) => ({
    // Custom divider
    divider: {
        backgroundColor: "#f4ab2e",
        width: "60px",
        height: "5px",
        borderRadius: "10px",
    },
});

export default RestInfoStyle;
